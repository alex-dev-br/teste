services:
  postgres:
    image: postgres:16
    container_name: pg-restaurant-users
    restart: unless-stopped
    profiles: ["ide", "stack"]
    ports:
      - "${DB_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: "${DB_NAME:-restaurant_backend_db}"
      POSTGRES_USER: "${DB_USER:-restaurant_backend_user}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:-restaurant_backend_password}"
      TZ: "${TZ:-America/Sao_Paulo}"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -h 127.0.0.1 -p 5432 -U ${DB_USER:-restaurant_backend_user} -d ${DB_NAME:-restaurant_backend_db}" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    volumes:
      - db_data:/var/lib/postgresql/data

  problem-docs:
    build: ../problem-docs
    image: problem-docs:latest
    container_name: problem-docs
    profiles: ["stack"]
    ports:
      - "8081:80"

  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    image: restaurant-users-api:latest
    container_name: restaurant-users-api
    restart: unless-stopped
    profiles: ["stack"]
    depends_on:
      postgres:
        condition: service_healthy
      problem-docs:
        condition: service_started
    environment:
      # Defina no .env: SPRING_PROFILES_ACTIVE=dev (seeds) ou =prod
      SPRING_PROFILES_ACTIVE: "${SPRING_PROFILES_ACTIVE:-prod}"

      # Datasource container-para-container (DNS 'postgres' na rede do Compose)
      SPRING_DATASOURCE_URL: "jdbc:postgresql://postgres:5432/${DB_NAME:-restaurant_backend_db}"
      SPRING_DATASOURCE_USERNAME: "${DB_USER:-restaurant_backend_user}"
      SPRING_DATASOURCE_PASSWORD: "${DB_PASSWORD:-restaurant_backend_password}"
      JWT_SECURITY_TOKEN: "${API_SECURITY_TOKEN_SECRET}"
      JWT_EXPIRATION: "${API_SECURITY_TOKEN_EXPIRATION}"
      # Flags JVM e timezone
      JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=75 -XX:+UseSerialGC"
      TZ: "${TZ:-America/Sao_Paulo}"
    ports:
      - "${APP_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/actuator/health | grep -q UP"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s

volumes:
  db_data: {}
